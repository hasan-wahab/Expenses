enum Status { initial, loading, success, error, message }

enum LoginType { emailAndPassword, google }

enum SyncStatus { synced, pending, failed }

enum AddPropertyMode { add, update }

enum AddExpenseMode { fromNavBar, fromCard }

enum PersonalInfoMode { add, update }

enum ExportToPdfMode { thisMonth, lastQuarter, customRange }
